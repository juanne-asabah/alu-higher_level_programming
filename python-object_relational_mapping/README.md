# Python - Object-relational mapping

## Description
This project focuses on connecting a Python script to a MySQL database using the `MySQLdb` module, as well as introducing Object-Relational Mapping (ORM) design patterns using `SQLAlchemy`.

## Tasks
* **0-select_states.py**: Script that lists all states from the database `hbtn_0e_0_usa`.
