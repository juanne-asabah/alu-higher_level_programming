=======================
MyList Test Suite
=======================

Importing the Subclass Module:
    >>> MyList = __import__('1-my_list').MyList

-----------------------
Test present: check instantiation
-----------------------
Creating clean object instances to verify basic structure:
    >>> my_list = MyList()
    >>> type(my_list)
    <class '1-my_list.MyList'>

-----------------------
Test present: check inherits from list
-----------------------
Ensuring that inheritance rules match the standard list protocol:
    >>> isinstance(my_list, list)
    True
    >>> issubclass(MyList, list)
    True

-----------------------
Test present: check __str__
-----------------------
Verifying string representation for printing actions:
    >>> str(my_list)
    '[]'

-----------------------
Test present: check print_sorted() with empty list
Correct output - case: class creation, regular print and sorted print
Correct output - case: class creation
Correct output - case: class creation and regular print
-----------------------
Evaluating the behaviour of an empty structure:
    >>> print(my_list)
    []
    >>> my_list.print_sorted()
    []

-----------------------
Test present: check append()
Correct output - case: class creation, append and regular print
-----------------------
Adding a single tracking value to look for successful mutation:
    >>> my_list.append(42)
    >>> print(my_list)
    [42]

-----------------------
Test present: check print_sorted() with sorted append
Correct output - case: class creation, append, regular print and sorted print
-----------------------
Clearing and adding elements that are already strictly ordered:
    >>> my_list = MyList()
    >>> my_list.append(10)
    >>> my_list.append(20)
    >>> my_list.append(30)
    >>> print(my_list)
    [10, 20, 30]
    >>> my_list.print_sorted()
    [10, 20, 30]

-----------------------
Test present: check print_sorted() with not sorted append
Correct output - case: class creation, multiple appends, regular print and sorted print
-----------------------
Evaluating unsorted item insertions to see if the readout alters correctly:
    >>> unsorted_list = MyList()
    >>> unsorted_list.append(5)
    >>> unsorted_list.append(1)
    >>> unsorted_list.append(3)
    >>> print(unsorted_list)
    [5, 1, 3]
    >>> unsorted_list.print_sorted()
    [1, 3, 5]
    >>> print(unsorted_list)
    [5, 1, 3]

-----------------------
Test present: check print_sorted() with no sorted append with negative number
Correct output - case: class creation, multiple appends (negative number), regular print and sorted print
-----------------------
Inserting negative boundaries to ensure sorting sign-logic works:
    >>> mixed_list = MyList()
    >>> mixed_list.append(-10)
    >>> mixed_list.append(5)
    >>> mixed_list.append(-2)
    >>> mixed_list.append(0)
    >>> print(mixed_list)
    [-10, 5, -2, 0]
    >>> mixed_list.print_sorted()
    [-10, -2, 0, 5]
    >>> print(mixed_list)
    [-10, 5, -2, 0]

-----------------------
Test present: check print_sorted() returns a new list
-----------------------
Confirming that sorted() returns a fresh layout without mutating the base data:
    >>> static_list = MyList([3, 2, 1])
    >>> static_list.print_sorted()
    [1, 2, 3]
    >>> print(static_list)
    [3, 2, 1]
