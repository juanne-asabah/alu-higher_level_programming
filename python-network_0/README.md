# Python - Network #0

This directory contains introductory networking exercises using Bash and `curl` scripts. It covers HTTP requests (GET, POST, DELETE, OPTIONS), retrieving response headers, cookies, and measuring body data payloads.

